# Shared secret — never commit
secret.key
*.key

# Built binaries / CI output
/client/pbs-auth-client
pbs-auth-client
pbs-auth-server
/dist/

# Traefik example: ACME account + issued certificates
letsencrypt/
acme.json
.env

# nginx example: bring-your-own TLS certificate material
certs/

# OS cruft
.DS_Store

# AI-assist context, kept local (not published)
CLAUDE.md
