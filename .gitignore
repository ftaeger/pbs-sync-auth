# Shared secret — never commit
secret.key
*.key

# Built binaries / CI output
/client/pbs-auth-client
pbs-auth-client
pbs-auth-server
/dist/

# Traefik example: ACME account + issued certificates
letsencrypt/
acme.json
.env

# OS cruft
.DS_Store
