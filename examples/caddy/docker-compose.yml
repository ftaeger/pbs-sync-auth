# pbs-sync-auth behind Caddy, TLS terminated by Caddy (bring your own certificate).
#
# Requirements:
#   - ./secret.key present:  openssl rand -hex 32 > secret.key
#   - ./certs/fullchain.pem and ./certs/privkey.pem present
#   - cp .env.example .env   # set DOMAIN
#
# Usage:
#   docker compose up -d

services:
  caddy:
    image: caddy:2
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: ${DOMAIN}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./certs:/etc/caddy/certs:ro
    depends_on:
      - pbs-sync-auth

  pbs-sync-auth:
    image: ghcr.io/ftaeger/pbs-sync-auth:latest
    restart: unless-stopped
    # No published ports: only reachable through Caddy on the internal network.
    environment:
      PBS_AUTH_HOST: "0.0.0.0"
      PBS_AUTH_PORT: "8099"
      PBS_AUTH_SECRET: /run/secrets/pbs_auth_secret
    volumes:
      # Shared secret, mounted read-only. Identical to the client's secret.
      - ./secret.key:/run/secrets/pbs_auth_secret:ro
    read_only: true
    security_opt:
      - no-new-privileges:true
